spring:
  application:
    name: loans
  datasource:
    url: jdbc:mysql://localhost:3306/UdemyLoans
    username: merve
    password: Merve123.
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      javax:
        persistence:
          validation:
            mode: none
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
  config:
    import:
      - "application_qa.yml"
      - "application_prod.yml"
    profiles:
      active:
        - "qa"

application:
  version: 1.0.0

server:
  port: 8090

logging:
  level:
    org.springframework: error

build:
  version: "3.0"

loans:
  message: "Welcome to MeryilBank loans related local APIs "
  contactDetails:
    name: "Amaal Grega - Developer"
    email: "amaal@meryilbank.com"
  onCallSupport:
    - (452) 456-2176
    - (546) 764-8934